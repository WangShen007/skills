interface:
  display_name: "Skill Orchestrator"
  short_description: "Route one request across installed skills"
  default_prompt: "Use $skill-orchestrator to route this task across the right installed skills and return one integrated result."
policy:
  allow_implicit_invocation: false
